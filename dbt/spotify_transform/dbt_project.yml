name: 'spotify_transform'
version: '1.0.0'

profile: 'spotify_transform'

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  spotify_transform:
    staging:
      +materialized: view
    marts:
      +materialized: table